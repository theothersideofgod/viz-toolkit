import { VizTreeLeafDefDirective } from './viz-tree-leaf-def.directive';

describe('VizTreeLeafDefDirective', () => {
  it('should create an instance', () => {
    const directive = new VizTreeLeafDefDirective();
    expect(directive).toBeTruthy();
  });
});
