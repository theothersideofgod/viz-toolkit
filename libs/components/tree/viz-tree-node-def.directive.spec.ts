import { VizTreeNodeDefDirective } from './viz-tree-node-def.directive';

describe('VizTreeNodeDefDirective', () => {
  it('should create an instance', () => {
    const directive = new VizTreeNodeDefDirective();
    expect(directive).toBeTruthy();
  });
});
