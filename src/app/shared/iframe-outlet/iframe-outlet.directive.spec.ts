import { IframeOutletDirective } from './iframe-outlet.directive';

describe('IframeOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new IframeOutletDirective();
    expect(directive).toBeTruthy();
  });
});
